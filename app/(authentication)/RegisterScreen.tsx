// đây là trang đăng ký
import { Image, SafeAreaView, StyleSheet, Text, View } from "react-native";

const RegisterScreen = () => {
  return (
    <SafeAreaView>
      <View>Trang dăng ký chứa form</View>
    </SafeAreaView>
  );
};

export default RegisterScreen;
